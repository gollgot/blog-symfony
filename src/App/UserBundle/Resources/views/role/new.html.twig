{% extends 'base.html.twig' %}

{% block title %}Nouveau rôle{% endblock %}

{% block body %}
    <a href="{{ path('roles_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
    <br><br>

    <h1>Nouveau rôle</h1>

    {{ form_start(roleForm) }}
        {{ form_widget(roleForm) }}
        <button class="btn btn-success">Créer</button>
    {{ form_end(roleForm) }}

{% endblock %}
